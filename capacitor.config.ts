import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MiApp2022812',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
